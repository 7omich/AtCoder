#include <iostream>
#define REP(i,n) for(int i=0; i<n; i++)
using namespace std;

int main() {
    cin.tie(0);
    ios::sync_with_stdio(false);

    int T, N, M;
    int A[], B[];
    cin >> T >> N;
    REP(i, N) {
        
    }
    cout << y / x << '\n';
}
